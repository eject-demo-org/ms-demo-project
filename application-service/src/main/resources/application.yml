server:
  port: 8201
  
spring:  application:    name: APPLICATION-SERVICE  messages:    encoding: UTF-8
eureka:
  client:
   register-with-eureka: true
   fetch-registry: true
   service-url:
    defaultZone: http://localhost:8761/eureka/
  instance:
      hostname: localhost   
